info:
  name: Create trail from JSON body
  type: http
  seq: 28

http:
  method: POST
  url: http://localhost:3000/api/trails
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
