info:
  name: Returns all regions
  type: http
  seq: 26

http:
  method: GET
  url: http://localhost:3000/api/regions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
