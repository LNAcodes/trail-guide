info:
  name: Show single region with trails
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/regions/bavarian-alps
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
