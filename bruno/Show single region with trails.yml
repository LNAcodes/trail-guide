info:
  name: Show single region with trails
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/regions/:slug
  params:
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
