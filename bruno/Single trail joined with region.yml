info:
  name: Single trail joined with region
  type: http
  seq: 24

http:
  method: GET
  url: http://localhost:3000/api/trails/:slug
  params:
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
