info:
  name: Updates given field
  type: http
  seq: 30

http:
  method: PATCH
  url: http://localhost:3000/api/trails/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
